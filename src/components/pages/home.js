import React from 'react';
import ItemListContainer from '../components/ItemListContainer';

const Home = () => {
  return (
    <main>
      <ItemListContainer />
    </main>
  );
};

export default Home;
